# ImgSlicer V33 需求说明

本文基于 `mockup-v33.html` 和当前补充需求整理，目标是将最终设计稿转换为可执行的产品需求文档，供产品、设计和开发共同使用。

## 1. 产品概述

`ImgSlicer` 是一款面向电子照片、胶片扫描图、底片扫描图的桌面处理工具，核心能力是对图片进行自动识别、切分和人工微调，并支持多文件夹批量处理。

本版本的产品心智是：

- 以“文件夹批次”为主要处理对象
- 以“自动处理为主、手动调整为辅”为核心工作流
- 以“后台并发处理 + 前台单图精修”为主要使用方式
- 以“原图不修改、输出可追溯”为基础约束

## 2. 产品目标

- 降低大量照片逐张手工切分的成本
- 支持不同风格的电子照片，包括横向、竖向和尺寸不规则照片
- 支持多个文件夹批次同时处理，并清晰展示各自进度
- 支持自动识别与人工微调结合的工作模式
- 在不修改原始照片的前提下完成输出
- 保证切分后的结果能够追溯到原始照片来源
- 支持 macOS 桌面环境下稳定运行

## 3. 目标用户

- 胶片摄影工作室
- 照片扫描与归档人员
- 需要批量整理电子照片的后期处理人员
- 有大量图片切分需求的个人用户

## 4. 核心需求

### 4.1 图片类型适配

系统需要支持多种照片形态，而不能仅针对规则图片：

- 支持横向照片
- 支持竖向照片
- 支持尺寸比例不规则的照片
- 支持不同风格、不同构图、不同留白的电子照片

### 4.2 文件夹导入与识别

系统需要支持以文件夹为主的导入方式：

- 支持通过按钮导入单个文件夹
- 支持通过按钮导入目录
- 支持将待处理文件夹或目录直接拖拽到界面中导入
- 系统导入后需自动识别其中有哪些可处理照片
- 若目录下包含多个子文件夹，系统需递归遍历并识别其中的照片

补充要求：

- 当用户拖入目录后，系统应提示识别出的文件夹数量、子文件夹数量、图片数量
- 不同子文件夹中的图片应保留来源关系，不能混为一个无结构集合

### 4.3 多任务并发处理

系统需要支持多个文件夹批次同时启动处理：

- 支持多组照片同时开始处理
- 每组任务独立运行，互不干扰
- 每组任务需展示独立状态
- 每组任务需展示独立进度
- 每组任务需展示当前处理说明
- 当并发任务超过可运行数量时，支持进入等待队列

### 4.4 自动与手动处理模式

系统需同时提供自动处理和手动处理两种方式：

- 默认处理方式为自动
- 系统应优先自动识别图片边界并生成裁切结果
- 对于特殊照片、识别困难照片或自动结果不理想的照片，用户可切换为手动调整
- 手动调整后，系统应保留人工结果，不应被自动结果直接覆盖

### 4.5 图片预览与微调

系统需要提供单张图片的精修能力：

- 展示当前图片原图
- 展示自动识别框或裁切框
- 支持拖拽四角或边缘进行微调
- 支持微调后重新计算或更新裁切结果
- 支持显示当前图片的处理反馈和状态说明

### 4.6 输出位置与目录规则

系统输出必须保持与原图目录的关联关系：

- 切分后的照片输出位置应与原始文件位置保持一致的上下文关系
- 系统必须创建新的输出文件夹保存结果
- 系统不得覆盖原始文件
- 系统不得移动原始文件
- 系统不得修改原始文件
- 若原始目录下有多个子文件夹，输出结果也需按子文件夹分别保存
- 不同来源文件夹的输出结果不能混放在同一结果目录中

### 4.7 输出命名规则

切分后的照片命名必须能关联到原始照片：

- 输出文件名必须能够追溯原始照片名称
- 建议输出文件名包含原始文件名主干
- 建议输出文件名附带切分序号或结果序号
- 若一张原图切分出多张结果，命名应保证唯一且可读

示例规则：

- `原图名_01`
- `原图名_slice_01`
- `原图名_part_01`

### 4.8 原图保护约束

系统必须遵守原图保护要求：

- 不得删除原始照片
- 不得覆盖原始照片
- 不得修改原始照片内容
- 不得改变原始照片所在目录结构
- 所有处理结果均写入新目录

## 5. 界面与信息架构

整体界面采用多区域工作台结构，用于同时承载批处理上下文和单图精修流程。

### 5.1 顶部区域

职责：承载品牌信息和窗口级语义。

包含：

- Logo
- 产品名称
- 产品副标题
- 窗口控制区

### 5.2 左侧区域：文件夹任务队列

职责：管理多个文件夹批次。

需要展示：

- 文件夹名称
- 图片总数
- 已处理数量 / 总数量
- 当前状态
- 当前处理说明
- 进度条

状态至少包括：

- `处理中`
- `等待中`
- `需确认`
- `已完成`

### 5.3 中间区域：当前图片工作区

职责：处理当前选中图片。

需要展示：

- 当前图片名称
- 当前所属文件夹
- 图片预览
- 识别框 / 裁切框
- 微调句柄
- 当前操作反馈

### 5.4 右侧区域：参数设置区

职责：设置当前识别和裁切规则。

需支持：

- 应用范围：当前 / 整个文件夹
- 图片方向：自动 / 横向 / 纵向
- 边距设置：上 / 下 / 左 / 右
- 边缘识别灵敏度
- 自动 / 手动处理状态体现

### 5.5 底部区域：图片胶片栏

职责：展示当前文件夹内图片序列。

需支持：

- 当前文件夹内图片切换
- 当前图片高亮
- 图片状态展示
- 左右导航

## 6. 核心流程

### 6.1 导入与建队列

1. 用户通过按钮选择文件夹/目录，或直接拖拽到界面
2. 系统扫描目录和子目录中的图片
3. 系统按文件夹维度建立任务组
4. 系统展示任务列表和初始统计信息

### 6.2 自动处理流程

1. 用户启动处理
2. 系统对每组照片执行自动识别
3. 系统更新每组任务进度
4. 用户可随时查看当前处理状态

### 6.3 手动修正流程

1. 用户选择某个任务组
2. 用户在底部胶片栏切换当前图片
3. 用户检查自动识别结果
4. 若结果不理想，用户拖拽裁切框进行修正
5. 系统保存人工调整结果

### 6.4 输出流程

1. 系统根据原始目录创建新的输出目录
2. 系统将切分结果保存到对应新目录
3. 系统按原始文件夹或子文件夹关系保存输出结果
4. 系统使用可追溯命名规则输出文件

## 7. 功能清单

### 7.1 导入能力

- 支持导入文件夹
- 支持导入目录
- 支持拖拽导入
- 支持递归扫描子文件夹

### 7.2 队列能力

- 支持多任务并发处理
- 支持等待队列
- 支持独立进度展示
- 支持独立状态展示

### 7.3 识别与切分能力

- 支持自动识别
- 支持手动调整
- 默认自动处理
- 支持特殊图片人工兜底

### 7.4 输出能力

- 输出到新文件夹
- 输出与原路径保持关联
- 输出命名可追溯
- 子文件夹结果分开保存

### 7.5 安全能力

- 不修改原始照片
- 不覆盖原始照片
- 不删除原始照片

## 8. 非功能需求

- 需要支持当前 macOS 系统
- 桌面端操作需适合长时间连续使用
- 大图预览需流畅
- 拖拽反馈需稳定及时
- 队列进度更新需低延迟
- 多任务并发时不应阻塞当前图片编辑
- 状态文案、颜色、进度条表达需统一

## 9. 关键约束

- 默认处理模式为自动
- 手动调整是自动识别失败时的正式兜底能力
- 原始照片始终只读处理
- 所有输出均写入新目录
- 输出结果必须可追溯原始来源
- 需要兼容当前 macOS 使用场景

## 10. 开发实现建议

建议后续实现时按以下模块拆分：

- `AppShell`：整体布局
- `QueuePanel`：左侧任务列表
- `PreviewWorkspace`：中间图片工作区
- `ParameterPanel`：右侧参数区
- `Filmstrip`：底部图片胶片栏
- `DropZoneOverlay`：拖拽导入层
- `TaskRunner`：后台任务调度
- `OutputManager`：输出目录与命名管理

## 11. 一句话结论

`ImgSlicer` 需要做成一个支持多文件夹并发处理、默认自动识别、支持人工兜底、输出可追溯且绝不破坏原图的 macOS 桌面图片切分工作台。
